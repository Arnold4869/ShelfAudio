<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 基础主题。
         注意 parent 必须是深色（DayNight / Dark）：
         原来是 Theme.AppCompat.Light.DarkActionBar —— 浅色主题的窗口底色是白色，
         状态栏/导航栏区域会露出白色，看起来像页面上下各多了一条空白行。
         同时把 windowBackground 设成页面底色，边缘区域与页面同色。 -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/appBackground</item>
        <!-- 状态栏/导航栏透明，内容延伸到边缘；安全区由 CSS env() 处理 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- 导航栏不再透明：透明时 WebView 之下的窗口区域透出来，
             老机型上该区域没有内容 → 视觉上就是 dock 底下一条"没填充"的暗带
             （小米 8SE 实测）。直接涂成 dock 底色 #171436，上下连成一整块。
             手势条本身是系统画的，在深色导航栏上自动反白，仍清晰可见。 -->
        <item name="android:navigationBarColor">@color/navBar</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/appBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/navBar</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
